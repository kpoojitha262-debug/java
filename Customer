public class Customer implements Comparable<Customer>
	{
	private int accountNumber;
	public String customerName;
	private double balance;
	private String accountType;

	public Customer(int accountNumber, String customerName, double balance, String accountType) {
		this.accountNumber = accountNumber;
		this.customerName = customerName;
		this.balance = balance;
		this.accountType = accountType;
	}
	public int compareTo(Customer customer)
	 {
		if(this.balance > customer.balance) 
		{
			return 1;
		}
		else if(this.balance < customer.balance)
		{
			return -1;
			
		}
		else 
		{
			return 0;
		}
}
	void display()
	{
		System.out.println("Account Number :" + accountNumber);
		System.out.println("Customer Name :" + customerName);
	    System.out.println("Account Balance : " + balance);
	    System.out.println("Account type : " + accountType);
	}
	}

//Test.java

import java.util.ArrayList;
import java.util.Collections;
public class Test {
public static void main(String[] args) {
	ArrayList<Customer> a1 = new ArrayList<Customer>();
	a1.add(new Customer(101, "John" , 85000, "Savings"));
	a1.add(new Customer(102, "James", 35000, "Savings"));
	a1.add(new Customer(103, "Ram" , 42000, "Savings"));
	a1.add(new Customer(104, "Ram" , 65000, "Savings"));
	a1.add(new Customer(105, "Ravi" , 85000, "Savings"));
	
	for(Customer c : a1)
	{
		c.display();
		
	}
	 Collections.sort(a1);
	 System.out.println("After sorting ::");
	
	for(Customer c : a1)
		
		
	{
		c.display();
	}
}
}


